//All the code is inside the server.js file
//Bad code style coz its our first node.js coding course
The UI is simple but contains all the info.The customers places the order which includes customer info and order info as well.
Then the request will go to the our local database and the server tells the factory to start producing.
The command which instructs robots to operate stores in the knowledge base, therefore, the server is communicating with the knowledge base and with robot all the time.
Once the product is finished, the product will be unloaded.
